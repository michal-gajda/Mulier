namespace Mulier.Domain.Common.Constants;

public static class DomainErrorCode
{
    public static readonly string INVALID_NAME = nameof(INVALID_NAME);
    public static readonly string INVALID_TITLE = nameof(INVALID_TITLE);
    public static readonly string NOT_FOUND = nameof(NOT_FOUND);
}
